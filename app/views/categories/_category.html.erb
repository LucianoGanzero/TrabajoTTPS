<div id="<%= dom_id category %>">
  <div style="display:flex; justify-content:center; self-align:center;width: 100%;">
  <div style="width: 70%;">
    <h4>Nombre</h4>
    <p><%= category.name %></p>

    <h4 class="mt-5">Productos asociados</h2>
    <% if category.products.any? %>
      <ul>
        <% category.products.each do |product| %>
          <li><%= product.name %></li>
        <% end %>
      </ul>
    <% else %>
      <p>No hay productos asociados a esta categoría.</p>
    <% end %>

     <% if category.sizes.any? %>
    <h4>Talles</h4>
    <div style="display: flex; flex-wrap: wrap;">
      <% category.sizes.order(:size).each do |size| %>
        <div class="p-2 border m-2" style="min-width: 100px; text-align: center;">
          <%= size.size %>
        </div>
      <% end %>
    </div>
  <% end %>
  </div>
  </div>

 
</div>
