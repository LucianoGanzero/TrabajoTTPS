<%= turbo_frame_tag "cart" do %>
    <% if @cart.orders.count > 0 %>
        <%= render "cart/orders" %>
        <p>Total: <%= @cart.total %></p>
    <% else %>
        <p>Your cart is empty</p>
    <% end %>
<% end %>